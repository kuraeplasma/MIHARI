{
  "name": "mihari",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "web",
    "services/dispatcher",
    "services/worker"
  ],
  "scripts": {
    "dev:web": "npm --prefix web run dev",
    "dev:dispatcher": "npm --prefix services/dispatcher run dev",
    "dev:worker": "npm --prefix services/worker run dev",
    "build:web": "npm --prefix web run build",
    "build:dispatcher": "npm --prefix services/dispatcher run build",
    "build:worker": "npm --prefix services/worker run build",
    "build:lp": "node scripts/build-lp.cjs"
  },
  "dependencies": {
    "next": "^14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
